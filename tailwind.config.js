/** @type {import('tailwindcss').Config} */
export default {
  // enable class-based dark mode so users can toggle theme
  darkMode: 'class',
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}
